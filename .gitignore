# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Node.js
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Virtual environments
.venv
.venv312
.fungus_cache
.kilocode
.vsc
.fungus_cache/
.kilocode
.pytest_cache
enterprise_index
embedding_cache
external
models
.env\n.env

.env

# Graphify code-graph output (local dev tool, ~MB) — rebuilt via `graphify update .`, never commit.
graphify-out/
.graphifyignore

# Private vault-derived benchmark artifacts (personal notes) - NEVER commit.
benchmarks/results/vault/

# vibemind-os/brain-derived benchmark artifacts (private source text + embeddings)
# on a PUBLIC remote - keep local, never commit. Rebuild via build_local_snapshot.
benchmarks/gate2/manifests/brain-*
benchmarks/results/gate2/*brain*
